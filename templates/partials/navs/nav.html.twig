<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href= {{ path('home') }}>Garage</a>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link {{ service ?? "" }}" aria-current="page" href={{ path('service') }}>Service</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ usedCar ?? "" }} " href={{ path('usedCar') }}>Nos Voiture Occassion </a>
                </li>
            </ul>
        </div>
        <div class="d-flex form-inline my-2 my-lg-0">
            {% dump(app.user) %}
            {% if app.user %}
            <div class="dropdown my-2 my-sm-0">
                <button class="btn btn-outline-primary  dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Administration
                </button>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="{{ path('admin_avis') }}">Avis</a></li>
                    <li><a class="dropdown-item {{ adminUsedCar ?? "" }}" href="{{ path('usedCar.view') }}">voiture d'occassion</a></li>
                    <li><a class="dropdown-item" href="#">Contact</a></li>
                    <li><a class="dropdown-item" href="#">Service</a></li>

                    {% if is_granted('ROLE_DIRECTEUR')  %}
                        <hr>
                        <li><a class="dropdown-item" href="{{ path('app_admin_compte') }}">Gerer les compte</a></li>
                    {% endif %}
                </ul>
                {% endif %}
            </div>
            {% if app.user %}
                <a class="btn btn-outline-danger my-2 my-sm-0 ms-4" href={{ path('app_logout') }}>Déconnection</a>
            {% else %}
                <a class="btn btn-outline-danger my-2 my-sm-0 ms-4" href={{ path('app_login') }}>Connection</a>
            {% endif %}
        </div>
    </div>
</nav>