
<div class="row">
    {%for avis in lstAvis %}
    <div class="col-sm-6 mb-3 mb-sm-0">
        <div class="card mb-3">
            <div class="card-body">
                <h5 class="card-title">{{ avis.nom }}</h5>
                <p class="card-text">{{ avis.commentaire }}</p>
                <p class="card-footer">note : {{ avis.note }}/5</p>
            </div>
        </div>
    </div>
    {% endfor %}
</div>