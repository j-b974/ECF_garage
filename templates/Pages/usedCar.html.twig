{% extends 'base.html.twig' %}

{% block title %}Voiture d'Occassion{% endblock %}

{% block body %}

    <style>

    </style>
    <h1 class="text-center text-white mt-4"> Voiture d'occassion</h1>

    <div class="row d-flex flex-column flex-sm-row bg-white mb-4 relative">
        <div class="d-flex flex-column flex-sm-col flex-md-col flex-lg-row  justify-content-between align-items-center">
            {% for  range in listeRanges %}
                <div class="col-xl-3 col-lg-3 col-md-4 col-sm-12">
                    {{ range.render() }}
                </div>
            {% endfor %}
        </div>
        <a class="btn btn-dark mb-2 " id="btnFiltre" data-link ="{{ path('data.usedCar') }}" >Validez</a>
    </div>

    {% include 'partials/cards/usedCarCard.html.twig' %}
    <div class=" d-flex justify-content-center mb-4" id="pagination">
        {{ knp_pagination_render(lstCar) }}
    </div>

{% endblock %}
{% block endJavascripts %}
    <script src="{{ asset('asset/js/usedCarFiltre.js') }}"  crossorigin="anonymous" defer></script>
{% endblock %}
