version: '3.8'

services:
  web:
    build:
      dockerfile: Dockerfile
    image: image_garage
    env_file:
      - .env
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.app.rule=Host(`garage.bertil.re`)"
      - "traefik.http.routers.app.entrypoints=web,websecure"
       # Indique à Traefik le port interne sur lequel le service écoute (ici 80)
      - "traefik.http.services.app.loadbalancer.server.port=80"
      - "traefik.docker.network=webTraefik"  # Réseau Docker utilisé par Traefik
    volumes:
      - .:/var/www/
    depends_on:
      - db
    ports:
      - "8000:80"
    networks:
      - webTraefik
  db:
    image: mysql:8.0
    container_name: db_garage_ecf
    restart: always
    volumes:
      # espace memoire pour mysql
      - db-data:/var/lib/mysql
      # ajoute les fichier certif en le ssl container

    environment:
      MYSQL_ROOT_PASSWORD: garage1234
      MYSQL_DATABASE: ecfgarage
      MYSQL_USER: adminGarage
      MYSQL_PASSWORD: 1234
    ports:
      - "3306:3306"
    networks:
      - webTraefik
  phpmyadmin:
    image: phpmyadmin:latest
    container_name: admin_bd_garage
    environment:
      PMA_HOST: db
      PMA_PORT: 3306
      PMA_ARBITRARY: 0
      PMA_ABSOLUTE_URI: http://localhost/SQLmyAdmin/
    ports:
      - "8080:80"
    restart: always
    volumes:
      #- ./PhpMyAdminConfig/config.inc.php:/etc/phpmyadmin/config.inc.php
      - ./certificatBdd:/etc/mysql/ssl
      - ./config.user.inc.php:/etc/phpmyadmin/config.user.inc.php
    depends_on:
      - db
    labels:
      - "traefik.enable=true"
      # Redirige les requêtes sur Host 'localhost' avec le chemin commençant par /SQLmyAdmin vers ce service
      - "traefik.http.routers.phpmyadmin.rule=Host(`localhost`) && PathPrefix(`/SQLmyAdmin/`)"  # Règle de routage
      - "traefik.http.routers.phpmyadmin.entrypoints=websecure"
      #- "traefik.http.routers.phpmyadmin.tls.certresolver=letsencrypt"
      - "traefik.http.middlewares.phpmyadmin-strip.stripprefix.prefixes=/SQLmyAdmin/"
      - "traefik.http.routers.phpmyadmin.middlewares=phpmyadmin-strip"
      - "traefik.http.routers.phpmyadmin.entrypoints=web"
      - "traefik.http.services.phpmyadmin.loadbalancer.server.port=80"
      - "traefik.docker.network=webTraefik"  # Réseau Docker utilisé par Traefik
    networks:
      - webTraefik
volumes:
  db-ssl:
  db-data:

networks:
  webTraefik:
    external: true  # Utiliser le réseau externe créé par Traefik